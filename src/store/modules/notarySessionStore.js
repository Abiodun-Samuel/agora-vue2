// import axios from "axios";
// import Vue from "vue";

const state = () => ({
  notary: "abiodun",
});

// getters
const getters = {};

// mutations
const mutations = {
  // SESSION_DETAILS(state, payload) {},
};

// actions
const actions = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
};
